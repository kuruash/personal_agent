import SwiftUI

enum AppColors {
    static let contentBackground = Color(nsColor: .windowBackgroundColor)
    static let inputBackground = Color(nsColor: .controlBackgroundColor)
    static let cardBackground = Color(nsColor: .controlBackgroundColor).opacity(0.72)
    static let subtleBorder = Color.primary.opacity(0.10)
    static let focusBorder = Color.accentColor.opacity(0.55)
}
